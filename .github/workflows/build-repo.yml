---
name: Build Pacman Repo

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  schedule:
    - cron: "0 0 * * *"

concurrency:
  group: "${{ github.workflow }}-${{ github.ref }}"
  cancel-in-progress: true

jobs:
  build-pacman-repo:
    runs-on: ubuntu-latest
    environment: prod
    container:
      image: archlinux:base-devel
    steps:
      - name: Container Setup
        run: |
          pacman -Syu --disable-download-timeout --needed --noconfirm \
            archlinux-keyring \
            base-devel \
            git \
            reflector \
            wget \
            rust \
            tree

      - name: Checkout Pacman Repo Builder
        uses: actions/checkout@v6
        with:
          repository: pacman-repo-builder/pacman-repo-builder
          path: pacman-repo-builder

      - name: Checkout yay
        uses: nick-fields/retry@v3
        with:
          timeout_minutes: 10
          max_attempts: 3
          retry_wait_seconds: 10
          command: |
            git clone https://aur.archlinux.org/yay.git yay

      - name: Checkout this repo
        uses: nick-fields/retry@v3
        with:
          timeout_minutes: 10
          max_attempts: 3
          retry_wait_seconds: 10
          command: |
            git clone --recurse-submodules https://github.com/${{ github.repository }}.git repo || \
            (cd repo && git submodule update --init --recursive)

      - name: Get GitHub App token for GitHub Pages
        id: app-token
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ secrets.PAGES_BOT_APP_ID }}
          private-key: ${{ secrets.PAGES_BOT_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}
          repositories: ${{ github.repository_owner }}.github.io

      - name: Checkout GitHub Pages Repo
        uses: actions/checkout@v6
        with:
          repository: ${{ github.repository_owner }}/${{ github.repository_owner }}.github.io
          ref: main
          path: github-pages
          token: ${{ steps.app-token.outputs.token }}
          persist-credentials: true
          fetch-depth: 0

      - name: Build and Install build-pacman-repo
        run: |
          sed -i 's/alpm = "[^"]*"/alpm = "*"/' Cargo.toml
          cargo update -p alpm --aggressive
          cargo build --release || (cargo fix --lib -p pacman-repo-builder && cargo build --release)
          install -Dm755 target/release/build-pacman-repo /usr/local/bin/build-pacman-repo
        working-directory: pacman-repo-builder

      - name: Patch makepkg
        run: |
          build-pacman-repo patch-makepkg --replace --unsafe-ignore-unknown-changes

          # Use maximum zstd compression to reduce package sizes
          sed -i "s/COMPRESSZST=.*/COMPRESSZST=(zstd -c -T0 --ultra -20 -)/" /etc/makepkg.conf
          
          # Disable debug packages globally (they're huge and exceed GitHub's 100MB limit)
          sed -i 's/OPTIONS=.*/OPTIONS=(strip docs !libtool !staticlibs emptydirs zipman purge !debug lto)/' /etc/makepkg.conf
        working-directory: repo

      - name: Install yay
        run: |
          makepkg -si --noconfirm
        working-directory: yay

      - name: Prepare sources and update SRCINFO
        run: |
          # update manifest with workspace path first (needed for outdated check)
          sed -i "s|repository: .*|repository: $GITHUB_WORKSPACE/github-pages/archlinux/custom.db.tar.gz|" build-pacman-repo.yaml

          # Fetch sources for packages with pkgver() so version can be determined
          for dir in pkgbuilds/*/; do
            if grep -q "^pkgver()" "$dir/PKGBUILD" 2>/dev/null; then
              echo "📥 Fetching sources for $(basename "$dir")..."
              (cd "$dir" && makepkg -od --nobuild --noconfirm) || true
            fi
          done

          # Update .SRCINFO files with accurate versions
          echo "🔄 Updating .SRCINFO files..."
          build-pacman-repo sync-srcinfo --update

          # echo all .SRCINFO files for debugging
          echo "📄 Current .SRCINFO files:"
          find pkgbuilds -name ".SRCINFO" -exec cat {} \;

          # now clean up any downloaded sources to save space
          for dir in pkgbuilds/*/; do
            # do a clean to remove sources
            if [ -f "$dir/PKGBUILD" ]; then
              echo "🧹 Cleaning sources for $(basename "$dir")..."
              (cd "$dir" && makepkg -odc --noconfirm) || true
            fi
          done
        working-directory: repo

      - name: Check for outdated packages
        id: check-outdated
        run: |
          echo ""
          echo "🔍 Debug: Existing packages in repo directory:"
          ls -1 "$GITHUB_WORKSPACE/github-pages/archlinux/"*.pkg.tar.zst 2>/dev/null | xargs -I{} basename {} || echo "  (none)"

          echo ""
          echo "🔍 Debug: Running outdated check with full details:"
          build-pacman-repo outdated --details lossy-yaml || true

          echo ""
          OUTDATED=$(build-pacman-repo outdated --details pkgname)
          if [ -z "$OUTDATED" ]; then
            echo "✅ All packages are up-to-date, nothing to build"
            echo "has_outdated=false" >> $GITHUB_OUTPUT
          else
            echo "📦 Outdated packages to build:"
            echo "$OUTDATED"
            echo "has_outdated=true" >> $GITHUB_OUTPUT
          fi
        working-directory: repo
        env:
          GITHUB_WORKSPACE: ${{ github.workspace }}

      - name: Build Pacman Repo
        if: steps.check-outdated.outputs.has_outdated == 'true'
        run: |
          # update mirrorlist to use fastest mirrors
          reflector --latest 10 --protocol http,https --sort rate --save /etc/pacman.d/mirrorlist

          # ensure github-pages/archlinux directory exists, if not, we can't copy files later
          test -d "$GITHUB_WORKSPACE/github-pages/archlinux" || (echo "cannot find the gh pages repo, exiting" && exit 1)

          # run the build, fail if build fails, and show pkgbuilds tree for debugging
          build-pacman-repo build || (echo "build-pacman-repo failed" && tree -lah pkgbuilds/ && exit 2)
        working-directory: repo
        env:
          GITHUB_WORKSPACE: ${{ github.workspace }}

      - name: Verify packages and update database
        if: steps.check-outdated.outputs.has_outdated == 'true'
        run: |
          REPO_DIR="$GITHUB_WORKSPACE/github-pages/archlinux"
          DB_FILE="$REPO_DIR/custom.db.tar.gz"

          # build-pacman-repo already copies packages to the repo directory
          # Just verify they're there and ensure database is up to date
          echo "📦 Packages in repository:"
          ls -lah "$REPO_DIR/"*.pkg.tar.zst 2>/dev/null || echo "  (none found)"

          PKG_COUNT=$(find "$REPO_DIR" -maxdepth 1 -name "*.pkg.tar.zst" | wc -l)
          if [ "$PKG_COUNT" -eq 0 ]; then
            echo "ERROR: No packages found in repository"
            exit 3
          fi

          echo "✅ Found $PKG_COUNT package(s):"
          find "$REPO_DIR" -maxdepth 1 -name "*.pkg.tar.zst" -exec basename {} \;
        env:
          GITHUB_WORKSPACE: ${{ github.workspace }}
        working-directory: repo

      - name: GitHub Commit & Push
        if: steps.check-outdated.outputs.has_outdated == 'true' && (github.event_name == 'schedule' || github.event_name == 'push')
        uses: actions-js/push@v1.5
        with:
          author_email: ${{ secrets.GH_BOT_EMAIL }}
          author_name: ${{ secrets.GH_BOT_NAME }}
          repository: ${{ github.repository_owner }}/${{ github.repository_owner }}.github.io
          branch: main
          directory: github-pages
          github_token: ${{ steps.app-token.outputs.token }}
          message: "Update per ${{ github.event_name }}: ${{ github.sha }}"
